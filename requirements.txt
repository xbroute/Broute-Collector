# Core collector logic uses the Python standard library.
# cryptography is used only to keep admin-added subscription URLs encrypted at
# rest in this public repository's telegram-bot-state branch.
cryptography>=42
